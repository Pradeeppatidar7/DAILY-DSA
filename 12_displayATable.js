// displaying a table in the console

const table = (n) => {
    for(let i = 1; i <= 10; i++){
        console.log(n*i);
    }
}

table(2); // will display the table of 2
table(17); // will display the table of 17
