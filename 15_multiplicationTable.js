// displaying a table in the console

const table = (n) => {
    for(let i = 1; i <= 10; i++){
        console.log(n*i);
    }
}

table(45); // will display the table of 45
table(23); // will display the table of 23